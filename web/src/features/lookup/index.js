// Unlike themes/build_now, Lookup is not role-gated - it needs no account
// at all, so there is no FEATURE_KEY here for App.jsx to check against
// /api/features.
export { LookupPage } from './LookupPage.jsx'
